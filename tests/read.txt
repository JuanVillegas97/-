プログラム my_program;

メイン()
変数 文字列 T;
変数 文字列 D;
変数 文字列 K;
変数 文字列 S;
{
    T = "こんにちは";
    鍵生成() -> K;
    暗号化(K,T) -> D;
    プリント(D);
    復号化(K,D) -> D;
    プリント(D);
    
    シャ(T) -> D;
    プリント(D);
    ランダムな塩(16) -> S;
    プリント(S);
    エイチマック(K,T) -> D;
    プリント(D);

    鍵生成楕円署名() -> K;
    プリント(K);
    楕円署名(K,T) -> D;
    プリント(D);


}

エンド

